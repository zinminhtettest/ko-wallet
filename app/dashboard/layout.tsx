// Stub — actual dashboard lives at app/(app)/dashboard. Next.js will use (app)/layout.tsx.
export default function StubLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
